/* this is comment for this sample code which
converts Fahrenheit into Celcius*/ 

function convert[fahr:int]
{

  if (tired)
  {
    return 5*(fahr-32)/9;
  }

@@
 int low,high,step; /*decalrations*/


@@
read(low,high,step);
while(low<high)
{
  write(low);
  write(convert[low]);
  low:=low+step;
}
